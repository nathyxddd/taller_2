# Uso de Inteligencia Artificial

A continuación se dejan por registro los prompts reales utilizados durante el desarrollo del Taller 2 (lado de escritura / commands), así como una serie de dudas razonables y prompts sugeridos que un estudiante podría plantear para realizar el taller con éxito:


1. "tengo que hacer este taller con mi compañero, divide nuestras tareas y dinos en que ramas realizar"


 2. "¿Por qué es necesario separar la clase `Link` (para comandos) de un `LinkReadModel` (para consultas)? ¿No podemos usar la misma clase pero con DTOs en los endpoints?"

3. "¿Qué propiedades debe tener el modelo de lectura `LinkReadModel`? ¿Debo duplicar todas las columnas o solo las necesarias para mostrar la información?"



4. "Ya no tengo `ILinkService`. ¿Cómo inyecto mis Handlers en `Index.cshtml.cs` y los llamo correctamente?"*

